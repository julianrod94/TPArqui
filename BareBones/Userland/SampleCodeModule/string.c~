
int strcmp(char * str1, char * str2) {
	
	int i = 0, j = 0, flag = 0;
	while(str[i] != 0 && str[j] != 0 && !flag) {
		flag += str[i++] - str[j++]; /* If the k-th character are different, flag is set */
	}
	return !flag && str[i] == 0 && str[j] == 0;

}